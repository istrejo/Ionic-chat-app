import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'firebase-chat-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
